No login. Open https://headers.kahwee.com. Import docs/examples/https-profile.json from github.com/kahwee/ch-header (starts off). Enable; approve the two test hosts. Reopen popup, select profile and enable again if needed. Reload test pages. Run checks and Check second site: only /headers/match shows hello-gecko/modified; other paths stay absent/original. Revoke all website access and repeat: all unchanged. Leave profiles off. Use demo values only.
